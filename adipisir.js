// Define a function named enqueue
function enqueue(item) {
    // Function implementation here
}

// Assign the function to a key named '_enqueue' in an object
const myObject = {
    _enqueue: enqueue,
};

// Accessing the function via the object
myObject._enqueue(someItem);
